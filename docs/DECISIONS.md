# Decisions log

One line per decision. Newest at the top. Never delete entries; supersede them with a new line.
Format: `YYYY-MM-DD | decision | why | alternative rejected`

2026-06-14 | SVC-3 caller-prepayment fix IMPLEMENTED and verified live, replacing the interim mainnet guard: POST /api/fx/swap now charges the caller the swap amount plus the 0.05 fee via dynamic x402 pricing (parse the body, price it, then gate), swaps the prepaid USDC to the target stable, and records only the fee as revenue, so the treasury stays whole. Verified end to end: a paid fx-swap of 0.1 USDC returned 200 with real swap tx 0x99b0ccdb6ce06efed845b090cc47ef14ce34a2a85526b280d38d0ff9420d46bc (USDC to USDM), settlement success, and the SpendTracker booked 0.05 revenue not 0.15; the fx-quote path still settles (regression pass). The request from field was dropped, the input is always the prepaid USDC | closes the CRITICAL treasury-drain finding with the real fix rather than a guard | shipping the guard as the permanent control or leaving the drain
2026-06-14 | Security audit run (full procedure; triggered by request and the submission freeze). CRITICAL found and mitigated: SVC-3 /api/fx/swap paid the swap output from the agent treasury while x402 collected only the 0.05 fee, so the caller never prepaid the swap amount and each call was a net payout bounded only by PayoutPolicy; mitigated by refusing fx-swap on any non-testnet network until caller-prepayment settlement is wired (testnet uses faucet funds; mainnet is real money and is approval-gated anyway); the proper fix is dynamic x402 pricing that charges amount plus fee. MEDIUM: the Telegram /notary command anchors on-chain paying the agent gas for free with no rate limit, a drain vector if the bot is enabled (mitigated, the bot is off by default). MEDIUM: x402 settles with waitUntil submitted so a service can respond before on-chain confirmation (required to avoid the facilitator 524; exposure is one capped call). LOW: replay guard and rate limiter are in-memory and reset on restart (documented in MOCKS.md). KaribuNotary.sol re-reviewed clean (permissionless by design, no external calls, no reentrancy). No secret value is logged anywhere | hardening pass before submission caught a real treasury drain | shipping SVC-3 as a live mainnet payout or leaving the bot gas drain silent
2026-06-14 | Self-funded gas in cUSD PROVEN: a KaribuNotary anchor (tx 0x81621ef288014c684b07f4e28db8f2eee0c5eda35578a3794539505ec9a8778c, status success, gasUsed 145770) paid gas in USDm via Celo CIP-64 feeCurrency; the USDm balance fell 0.000471 (the gas) while CELO was untouched (delta 0). thirdweb does not expose feeCurrency, so this path uses viem with its celo chainConfig resolved through the Mento dependency | proves the agent never needs native CELO to operate, a core selling point | claiming cUSD gas without a tx that actually paid it
2026-06-14 | x402 live settled payment PROVEN end to end: a paid POST /api/fx/quote returned 200 with a real Mento quote (1 USDM = 1.000030 USDC) after the client signed an EIP-3009 USDC authorization and the thirdweb facilitator settled it (settlement success true, network eip155:11142220, payer the agent); self-initiated and labeled. Two fixes were needed: pass the USDC asset explicitly in the settlePayment price (thirdweb cannot resolve a default asset for the custom Celo Sepolia chain, so a USD-string price returned empty requirements and a {} body), and set waitUntil submitted (the default confirmed wait exceeds the facilitator edge timeout and returned HTTP 524) | proves the paid-service loop, the headline Track 1 and Track 2 capability | leaving x402 unproven or faking a settlement
2026-06-14 | SVC-3 Mento swap PROVEN on-chain (real settled tx): the agent swapped 0.2 USDC to 0.19991 USDm via the Mento Router 0xcf6cd45210b3ffe3ca28379c4683f1e60d0c2ccd; swap tx 0x52b103df93f161dcdeecaa7f62d6ee34fa3815f78d3362fb93fd8b6421261bbf and approval tx 0xccab1b26def4c60259766c74d761a17059d2c24e25eedc70c5b32612af409ed0 both status success; balances confirmed USDC 20.0 to 19.8 and USDm 0 to 0.19991 (18dp); self-initiated with the agent's own Circle-faucet USDC | proves the buildSwapTransaction execution path end to end on Sepolia | presenting SVC-3 as code-only or faking a swap
2026-06-14 | Sepolia USDC/USDm Mento pool is thin: swaps fill only up to about 0.2 USDC, 0.5 USDC and above revert InsufficientLiquidity (0xbb55fd27) at estimation even though getAmountOut still quotes; forno read-lag reconfirmed a third time (immediate post-swap balanceOf returned stale zeros, a fresh re-read was correct), so the swap module must re-read after a short delay | sizes the testnet swap and explains the stale zero-delta | trusting the first balance read or assuming the pool is bottomless
2026-06-14 | THIRDWEB_SERVER_WALLET_ADDRESS set in .env (0xC55F4148F245970a2D3781574f3118739b4ACfaD, the thirdweb project gas-sponsored relayer); x402 settlement is now configurable and the first live settled call is the next proof | unblocks the paid path that returned 503 without it | leaving x402 unprovable
2026-06-13 | SVC-3 swap verification path investigated on-chain: CELO has no Mento route to any stable on Sepolia; USDC to USDm and axlUSDC to USDm routes exist (1 hop); the mock axlUSDC mint is owner-gated (OwnableUnauthorizedAccount 0x118cdaa7) so the agent cannot mint it | to verify the swap execution the agent needs a stable token; the one self-serve path is test USDC from faucet.circle.com to the agent address | minting the mock (owner-only) or swapping from CELO (no route)
2026-06-13 | SVC-3 treasury-safety core built: PayoutPolicy enforces the per-call and per-wallet daily FX payout caps (anonymous 0.25 and 1.0, verified 5.0 and 20.0, rolling 24h window), 6 unit tests (27 TS tests pass); the Mento swap execution wires on top when the market opens | the verifiable safety-critical part of the otherwise-blocked SVC-3; a test caught a wrong assumption and confirmed the strict per-call cap | building the unverifiable swap execution blind
2026-06-13 | docs/BRIEF.md written (the one-page brief from the plan layout): goal, deliverables, milestones, judging criteria, constraints, stack, demo pointer, out of scope | the last plan-listed doc deliverable, completes the docs set | leaving the docs set incomplete
2026-06-13 | Rate limiting added (Day 3 hardening): in-memory RateLimiter, 120 requests per minute per client IP, onRequest hook on every route except /health; verified by integration (120 OK then 429, health stays 200) and 4 unit tests (21 TS tests pass) | the named Day 3 rate-limit item; /health is exempt so uptime pings are never throttled | rate-limiting the health check or pulling a heavyweight dependency
2026-06-13 | Security and hardening pass (audit triggered): found and fixed an SSE fan-out bug. EventBus.emit now isolates each subscriber in try/catch and drops a failed one, so a dead client cannot break the feed or crash the emitter; added 3 EventBus tests (17 TS tests pass total); reviewed the contract, payment, notary, fx, spend-tracker, metrics, and config and found no further issues | a dead SSE connection was a real availability risk on the judge's dashboard | leaving the fan-out unguarded
2026-06-13 | TS unit tests added (Node built-in runner, no new dep): 14 tests over the trust boundaries (SpendTracker money caps and 80 percent stop, MetricsStore honesty exclusions including demo-wallet and self-initiated, ReplayGuard window), all pass; apps/agent test script is node --test dist/*.test.js | Day 3 hardening and the audit value of testing the trust boundaries | leaving the money and honesty logic untested; note that node --test on a directory also ran index.js, so the script scopes to the test glob
2026-06-13 | /verify-receipt/:hash route added (plan 2.5): reads the notary record on-chain, free and read-only, verified against the real anchor (hash 0x525ba05c.., anchorer the agent wallet, index 1); decoupled the thirdweb client and notary runtime from the x402 server wallet via buildThirdwebClient, so reads and anchoring work without it and health now reports notary:true | a referenced feature, now verifiable against real data, and correct separation of read from settlement | coupling on-chain reads to the settlement wallet
2026-06-13 | Replay protection added (plan 2.3): ReplayGuard rejects a duplicate client nonce within REPLAY_WINDOW_MS (24h), wired into the notary route (nonce optional, 409 on a duplicate); in-memory for now and noted in docs/MOCKS.md; build green | frozen-design requirement, reuses the state-contract constant | a persisted store before the demo, which MODE T does not need
2026-06-13 | docs/DEMO_SCRIPT.md and docs/SUBMISSION.md drafted: the 120-second timed demo with the real addresses and honesty notes, the registration tweet drafts, the celobuilders flow, and the submission checklist; drew records and posts | submission artifacts I produce and drew posts, the draft-then-post pattern | posting or submitting anything automatically
2026-06-13 | scripts/e2e.sh built and passes (E2E_OK, exit 0): forge test 17/17, install and build, idempotent deploy and register (agentId 358), a native-gas notary anchor with a forno-lag-safe re-read, and server health, skills, metrics reads; the x402 paid call is skipped with a clear message until the server wallet is set | the one-command repro the README references, idempotent so it exits 0 on re-run | a paid-call stub that pretends to settle
2026-06-13 | SVC-5 Telegram bot built (grammY): /start /help /quote (Mento) /notary (real anchor) /verify; gated behind TELEGRAM_BOT_ENABLED=1 so it never goes live without explicit opt-in; compiles and stays disabled by default (verified, the auto-mode classifier also blocked taking it live) | plan keep-priority human channel; respects the externally-visible stop rule | auto-starting the bot whenever the token is present
2026-06-13 | SVC-1 verify route added (x402-gated) behind a SelfVerifier interface; Self is stubbed on Sepolia with an honest note and is real on mainnet later; compiles and gates 503 without the server wallet | completes the 4-service HTTP surface; plan Day 1 item 5 permits the interface stub | a false-positive verify or blocking on Self ABI research
2026-06-13 | README.md drafted from measured facts: proof links (agentId 358, register tx, notary address, anchor tx, registry addresses), architecture diagram, sponsor rationale, repro steps, and the real-vs-mock honesty section; dash and banned-word clean | submission requirement, built early so it never drifts | a marketing README with unverified claims
2026-06-13 | Trust-plumbing routes added and verified: /.well-known/agent-registration.json (registry, agentId 358, chainId, the ERC-8004 domain-verification file) and /.well-known/agent-card.json (A2A card with the 4 service skills); agent now serves 9 routes | plan 2.2; marks the endpoint verified in explorers and discoverable over A2A | leaving the endpoint unverified and undiscoverable
2026-06-13 | karibu-skill client package built (SVC-6 install half): createKaribuClient discovers /api/skills (zod-validated) and pays verify, fxQuote, notary via wrapFetchWithPayment; errors as values; compiles strict against thirdweb 5.120.1, dist emitted; thirdweb is a peer dependency, zod a real dependency | completes the meta-play so any team installs and pays Karibu; live paid calls need the server wallet | bundling a wallet or hardcoding the catalog in the skill
2026-06-13 | SVC-6 discovery built: free GET /api/skills serves SERVICE_CATALOG (single source in state-contract) so any agent can discover Karibu's services and prices; verified returning the full manifest | Track 2 and 3 meta-play, the discovery half; the installable karibu-skill client package is next | duplicating the catalog inside the skill package
2026-06-13 | MOCK_STREAM=1 dev mode added: synthetic event generator (sample wallets 0x1111.., hashes 0xmock..) feeds /api/stream and metrics so the dashboard can be skinned; /health reports mockStream true; OFF for the real demo; documented in docs/MOCKS.md; verified emitting events with metrics moving | Day 2 first task, unblocks drew's dashboard in parallel | leaving the dashboard with no data to render
2026-06-13 | docs/MOCKS.md created (honesty ledger): every tx real, self-initiated labeled, drew's demo wallets excluded, no owner or operator feedback, no wash volume, testnet never presented as mainnet, MOCK_STREAM dev-only | plan section 9 honesty requirement | leaving the honesty claims undocumented
2026-06-13 | SVC-2 fx-quote built: Mento v3 SDK loaded via createRequire (CJS) behind x402; quoteFx returns distinct error values for unknown symbol, bad amount, closed market, missing route, stale feed; agent compiles strict and the Mento runtime initializes (health fx:true) | Day 2 FX quote service; gated live quote needs the thirdweb server wallet | a direct Broker integration instead of the SDK
2026-06-13 | Mento forex pairs follow standard forex hours (closed weekends, reopen Sunday about 5pm ET), so the Sunday demo window 5pm to 11pm ET is open for a live local-currency swap; USD-stable swaps (USDm to USDC) work at any hour | reframes the FX-closure finding as demo timing rather than a scope cut | invoking the risk-3 P2P fallback prematurely
2026-06-13 | Mento FX finding (read-only quotes): USDm to EURm reports "FX market is currently closed" on BOTH mainnet and Sepolia (forex trading hours, weekend); USDm to KESm reverts "no valid median" (Sepolia rate feed); USD-stable USDm to USDC quotes fine on Sepolia (1 USDm = 1.000098 USDC) | resolves the Mento liquidity OPEN; the Sunday demo cannot show a live local-currency FX swap during weekend hours | assuming Mento FX is available at any hour
2026-06-13 | No direct CELO to USDm Mento route on Sepolia or mainnet (v3 router returns "No route found") | measured via getAmountOut | planning to fund test USDm by a single CELO to USDm swap
2026-06-13 | Mento SDK 3.2.8 ESM build has extensionless internal imports that break strict Node ESM; the CJS build works, so the agent loads it through createRequire with a typed binding | ERR_MODULE_NOT_FOUND on import, success on require | importing the SDK as ESM
2026-06-13 | KaribuNotary anchor proven on-chain: test anchor tx 0x90c3aa9598964dc8d5689c9aa1e6c185cd7b49e6cd24f752fd7cb220242c7a4c (status 1), anchorCount 1, record stored (anchorer is the agent, index 1); self-initiated test anchor with native CELO gas | gate item (c) in native-gas form | the cUSD-gas form, which needs test USDm
2026-06-13 | forno Sepolia read replicas lag about 1 to 2 seconds after a write, so an immediate read can be stale | observed twice (register nonce, anchor count); e2e.sh and scripts must re-read or wait briefly after a write | trusting the first read after a transaction
2026-06-13 | x402 leg built: SVC-4 notary behind settlePayment (thirdweb facilitator); anchors on-chain via the agent account; compiles under strict TS; returns 503 until THIRDWEB_SERVER_WALLET_ADDRESS is set, then the 402 challenge, then settles | Day 1 gate item 4 code complete; settlement needs the thirdweb server wallet and a client holding test stable | a hand-rolled payment verifier instead of thirdweb x402
2026-06-13 | thirdweb pinned 5.120.1; x402 server is settlePayment(args) with network as a thirdweb Chain (celoSepolia), client is wrapFetchWithPayment(fetch, client, wallet, {maxValue}) positional | exact API read from the installed .d.ts, not the docs SPA | trusting the rendered docs which only showed the React hook
2026-06-13 | pnpm skips the three optional native addons bufferutil, keccak, utf-8-validate via ignoredBuiltDependencies; install exits 0 | they have pure-JS fallbacks and thirdweb works without them; avoids node-gyp builds | approving native builds that need a toolchain
2026-06-13 | Sepolia registration COMPLETE: agentId 358, owner is the agent wallet, tx 0xc200567358a8f56cd745b1d8ce9585652e95306ebe16c2024b22eb2be1d8caeb | Phase 0 spike done and Day 1 gate evidence (a) register on the Identity Registry | leaving the spike unfinished
2026-06-13 | KaribuNotary deployed to Sepolia at 0x42B8e75E43E28577E412026ceDb511b5B3cf0f41 (anchorCount reads 0) | Day 1 gate; the notary is live for anchoring | deploying only to mainnet
2026-06-13 | deploy-sepolia.sh split deploy and register across two runs due to RPC nonce lag (deploy used nonce 0, register retried at nonce 1) | forno returned a stale nonce right after the deploy; the idempotent re-run resolved it cleanly | an artificial sleep or manual nonce bookkeeping
2026-06-13 | apps/agent skeleton serving: Fastify /health, /api/metrics, /api/treasury, SSE /api/stream backed by MetricsStore, EventBus, SpendTracker, all consuming @karibu/state-contract; verified with live curl probes | Day 1 gate item 3; metrics start at zero and are measured, never hardcoded | premature x402 wiring before the skeleton serves
2026-06-13 | Dropped tsx from apps/agent (build with tsc, run with node) | pnpm 11 blocks esbuild build scripts and tsx was the only consumer; removing it keeps install at exit 0 with no build-script approval | approving esbuild builds and keeping the dev watcher
2026-06-13 | pnpm workspace scaffolded (root + packages/state-contract), builds green under strict TS; state-contract implements plan section 2.4 types as zod schemas with derived types, caps from 2.3, prices from 2.1 | Day 1 scaffold item 1; zod validates every boundary and types cannot drift from schemas | hand-written interfaces duplicated from the schemas
2026-06-13 | KaribuNotary.sol green: anchor(bytes32) with Anchored event and InvalidHash/AlreadyAnchored/NotAnchored errors; 17 Foundry tests pass (3 fuzz at 256 runs), zero compiler warnings; deploy gated on funding | Day 1 gate item 2; permissionless ownerless notary with no external calls means no reentrancy vector | an owner-gated or upgradeable notary
2026-06-13 | Foundry deps vendored by scripts/get-foundry-deps.sh (HTTP tarball of the latest forge-std release), lib/ gitignored | the agent never runs git and forge install uses git submodules | forge install / git submodules
2026-06-13 | Notary Record stores timestamp and index as uint256 (not packed uint64) | removes the unsafe-typecast truncation warning; one extra slot is acceptable | packing into uint64 with truncating casts
2026-06-13 | Sepolia register() proven by read-only eth_call simulation (would mint agentId 353, agentURI data URI 665 bytes); real send gated on funding | de-risk the ERC-8004 leg before spending gas | sending blind without simulating
2026-06-13 | Sepolia register() spike is paused on funding: agent balance is 0 and faucet.celo.org is browser/GitHub gated with no CLI API | faucets cannot be driven headless | scripting the faucet from the agent
2026-06-13 | Defer pnpm/nvm setup to the start of Day 1 scaffolding | corepack enable failed on the system node (root-owned /usr/bin); pnpm is not needed for the Phase 0 spike | installing pnpm via sudo into /usr/bin
2026-06-13 | Standards loaded from repo .claude/ (SKILL_GENERAL.md, REFERENCE_SECURITY_AUDIT.md) | the goal command paths ~/.claude/ and docs/standards/ do not exist on this machine | blocking on the missing canonical paths
2026-06-13 | Celo stables use Mento m-suffix branding now: USDm=cUSD, EURm=cEUR, KESm=cKES, BRLm=cREAL, same addresses, all 18 decimals (on-chain) | cast symbol()/decimals() reads on both networks | assuming the c-prefixed symbols still resolve on-chain
2026-06-13 | Keep the plan field names (revenueCusd, gasPaidCusd, cUSD copy) and map symbol-to-address via FACTS.md | the state-contract interface is frozen by the plan; renaming fields is banned | renaming fields to USDm and breaking the frozen contract
2026-06-13 | Karibu pays its own gas in USDm directly as feeCurrency (no adapter) | USDm is 18 decimals, confirmed on-chain | using the 6-decimal USDC path that needs an adapter
2026-06-13 | x402 (thirdweb) confirmed on BOTH celo and celoSepolia, tokens USDC/USDT/USDm | thirdweb x402 docs, Celo x402 page, thirdweb 26-chains changelog | assuming mainnet-only and blocking MODE T testnet work
2026-06-13 | Mento Broker mainnet 0x777A8255cA72412f0d706dc03C9D1987306B4CaD promoted to verified | cast code() shows a deployed contract | the draft status "do not use without verification"
2026-06-13 | ERC-8004 register path: register(string agentURI) returns uint256 agentId; read agentId from the Registered(agentId, agentURI, owner) event | confirmed against the repo ABI JSON | guessing the signature or using the no-arg overload
2026-06-13 | Agent wallet created 0x1147856217691a72C96F36F04697Abfb7305eF9f, key only in gitignored .env | plan section 4.5 (agent creates its own wallet) | drew creating the wallet manually
2026-06-13 | npm names karibu-skill and create-karibu-skill are available; default install stays npx github until drew approves a publish | registry 404 for both; publish is externally visible | publishing without approval
2026-06-13 | Custom TypeScript agent over OpenClaw | full control of the x402 gate, Self middleware, and SSE; any framework allowed | OpenClaw scaffold
2026-06-13 | Telegram bot library grammY | modern TypeScript API, good middleware model | Telegraf or node-telegram-bot-api
