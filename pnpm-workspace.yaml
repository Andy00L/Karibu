packages:
  - "packages/*"
  - "apps/*"
# These optional native addons (pulled transitively by thirdweb and the Mento
# SDK) have pure JavaScript fallbacks, so their build scripts are intentionally
# skipped rather than compiled with node-gyp.
allowBuilds:
  bufferutil: false
  keccak: false
  utf-8-validate: false
